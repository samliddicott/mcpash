# Bash COMPAT Delta Inventory

Source: `https://tiswww.case.edu/php/chet/bash/COMPAT`

This inventory captures the compatibility-level behavior deltas from the `Shell Compatibility Level` table (`compat31`..`compat52`).

Files:
- Requirements: `docs/specs/bash-compat-deltas-requirements.tsv`
- Matrix: `docs/specs/bash-compat-deltas-implementation-matrix.tsv`

Notes:
- Deltas are cumulative by design.
- We are not committing to support all historical levels yet.
- Rows are retained as feature-design and comparator-test inputs.

Counts:
- Levels parsed: 10
- Delta rows parsed: 29
